/**
 * @deprecated Use {@link dashboardAccessGuard} en su lugar.
 * Se mantiene por compatibilidad; delega la misma politica de acceso.
 */
export { dashboardAccessGuard as protectedPagesGuard } from './dashboard-access.guard';
